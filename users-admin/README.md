Users Admin (Standalone)

Scripts
- `npm install`
- `npm run dev` (default port 5176)
- `npm run build` / `npm run preview`

Environment
- `VITE_API_URL` (default: http://localhost:4000)

Backend endpoints required
- GET `/api/user/list`
- POST `/api/user/update`
- POST `/api/user/remove`

